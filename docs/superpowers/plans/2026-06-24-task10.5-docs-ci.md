# Task 10.5 Docs and CI Baseline Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make the GitHub repository accurately describe the post-Task-10 system state and add a minimal Python 3.11 pytest CI workflow.

**Architecture:** Documentation-only and CI-only change. Runtime code is not modified; config example paths are changed from local machine paths to safe placeholders, and tests are updated to match.

**Tech Stack:** Markdown, JSON configuration, GitHub Actions, Python 3.11, pytest.

## Global Constraints

- Work on branch `task10.5-docs-ci`.
- Do not change runtime behavior.
- Do not implement Task 11.
- Do not publish local models, data, real benchmark JSON, `config/local.json`, WAV, MP4, ONNX, or NPY files.
- Keep automated tests on mock/default paths; real provider smoke tests remain opt-in.

---

### Task 1: Repository-facing documentation

**Files:**
- Modify: `README.md`
- Modify: `项目说明goal.md`
- Modify: `阶段式目标.md`
- Create: `docs/status/2026-06-24-current-state.md`
- Modify: `docs/superpowers/plans/2026-06-23-task7-emotalk-segment-acceleration.md`
- Modify: `docs/superpowers/plans/2026-06-23-task8-emotalk-sidecar.md`

**Interfaces:**
- Produces: current status and next-task guidance for humans and web GPT readers.

- [x] Add current MVP status, benchmark baseline, and next P0 risks.
- [x] Mark old Task 7/8 assumptions as superseded.
- [x] Keep the next implementation target as Task 11, without implementing it.

### Task 2: Public example config cleanup

**Files:**
- Modify: `config/emotalk.example.json`
- Modify: `tests/unit/test_config.py`

**Interfaces:**
- Produces: public-safe example paths while preserving strict config parsing.

- [x] Replace `/home/user/...` paths with `/path/to/...` placeholders.
- [x] Update config tests to assert placeholders.

### Task 3: GitHub Actions pytest

**Files:**
- Create: `.github/workflows/pytest.yml`

**Interfaces:**
- Produces: CI workflow that installs `.[dev,client]` and runs `python -m pytest -q` on Python 3.11.

- [x] Add checkout, setup-python, dependency install, and pytest steps.

### Task 4: Verification

**Files:**
- All changed files.

**Interfaces:**
- Produces: verified docs/CI branch ready to merge and push.

- [ ] Run:

```bash
PYTHONPATH=src .venv/bin/python -m pytest -q
```

- [ ] Inspect:

```bash
git status --short
git diff --check
```

- [ ] Commit and push to GitHub.
