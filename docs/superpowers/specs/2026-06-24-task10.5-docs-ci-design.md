# Task 10.5 Docs and CI Baseline Design

## Goal

Task 10.5 updates the repository-facing project narrative after Tasks 5–10 and adds a minimal GitHub Actions pytest workflow so GitHub/web GPT readers see the current state rather than older pre-sidecar plans.

## Scope

Do:

- Update README as the GitHub entry point.
- Update `项目说明goal.md` and `阶段式目标.md` with a 2026-06-24 status calibration.
- Add a status document summarizing current capabilities, benchmark baseline, P0 risks, and next tasks.
- Mark old Task 7/8 plans as superseded where their assumptions differ from the implemented sidecar.
- Replace public example absolute paths in `config/emotalk.example.json` with placeholder paths.
- Add GitHub Actions pytest for Python 3.11.

Do not:

- Do not change runtime behavior.
- Do not change provider protocols.
- Do not add Task 11 implementation.
- Do not publish local `config/local.json`, models, data, WAV, MP4, ONNX, NPY, or benchmark artifacts.

## Current State Summary

The project is now an “实验室级准实时数字人 MVP”:

```text
HTTP /pipeline/audio: real chain available
WS /pipeline/stream: sentence-level incremental chain available
EmoTalk sidecar: warm path about 370–520ms
Face continuity: crossfade implemented
Eye continuity: framework implemented, default no-op
Stale-drop: old turn face leak count remains zero in benchmark
```

The next real implementation task is Task 11: cancellation correctness and ordered Face pipeline.

## Acceptance

```text
README and root planning docs describe Task 5–10 reality.
Old superseded plans are clearly marked.
config/emotalk.example.json no longer exposes local /home/user paths.
GitHub Actions runs Python 3.11 pytest.
Full local pytest passes.
```
